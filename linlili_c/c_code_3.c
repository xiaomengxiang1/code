#include <stdio.h>

// 在 C 语言中，双引号（"）用于表示字符串常量，而单引号（'）用于表示字符常量（单个字符）
// printf 的第一个参数是格式化字符串，它必须是一个字符串常量，因此需要使用双引号


//和python一样使用 \ 转义字符时

int main() {
    printf("His favorite song is \"Lemon tree\".\n");
    printf("C:\\Users\\Example");
    return 0;
}